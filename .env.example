# ============================================================
# Algeria Health & Wellness — variables d'environnement
# Copiez ce fichier en `.env.local` et renseignez ce dont vous avez besoin.
# ============================================================

# --- Couche IA -----------------------------------------------------
#
# TOUT EST FACULTATIF. Sans aucune clé, la plateforme fonctionne
# intégralement sur son moteur de règles déterministe : parcours,
# recommandations, estimations et concierge restent opérationnels.
#
# Renseigner une clé active le concierge conversationnel enrichi.
# Les garde-fous médicaux s'appliquent dans les deux cas.

# Clé Anthropic. Le SDK la lit automatiquement depuis cette variable.
# ANTHROPIC_API_KEY=sk-ant-...

# Modèle utilisé. Par défaut : claude-opus-5
# AI_MODEL=claude-opus-5

# Force le mode moteur de règles même si une clé est présente.
# Utile pour démontrer le comportement hors ligne.
# AI_PROVIDER=off


# --- Base de données (non branchée à ce stade) ---------------------
#
# Renseignée, les décisions de modération et le fil d'actualité
# survivent au redéploiement, et le dédoublonnage est arbitré par la
# base via deux index uniques. Absente, tout fonctionne en mémoire du
# processus : démontrable, mais rien ne survit à un redémarrage — et la
# page de modération l'affiche.
#
# Le schéma se crée seul au premier appel. Aucune migration à lancer.
#
# DATABASE_URL=postgresql://user:password@localhost:5432/algeria_health


# --- Stockage documentaire (non branché à ce stade) ----------------
#
# Le coffre documentaire est actuellement une démonstration côté
# navigateur. Une mise en production impose un stockage chiffré.
#
# S3_ENDPOINT=
# S3_BUCKET=
# S3_ACCESS_KEY_ID=
# S3_SECRET_ACCESS_KEY=


# --- Fil d'actualité -----------------------------------------------
#
# La veille fonctionne sans aucune de ces variables : elle lit alors les
# flux de presse et les soumissions de partenaires, et rien de plus.

# Voie de secours pour ce qui n'a pas de session : tâche planifiée, script
# d'exploitation. La voie normale est un compte dont le rôle atteint
# « modérateur » — créez-le sur /compte, le premier compte administre.
# ⚠️ Sans session ET sans ce jeton, l'administration est REFUSÉE.
# ADMIN_TOKEN=une-chaine-longue-et-aleatoire

# Signature des appels de la tâche planifiée Vercel Cron.
# CRON_SECRET=une-autre-chaine-longue-et-aleatoire

# Active le collecteur de recherche web. Sans clé, il reste inactif.
# Clé à créer sur https://brave.com/search/api/
# BRAVE_SEARCH_API_KEY=
